id: brief
title: Brief
kind: markdown
description: Task brief — problem, goal, user scenarios, edge cases, acceptance criteria.
default_template: templates/default.md

front_matter:
  type: object
  required: [status, summary]
  properties:
    status:
      type: string
      enum: [draft, approved]
    summary:
      type: string

validation:
  required_sections:
    - Problem
    - Goal
    - Scenarios
    - Edge cases
    - Acceptance criteria
  required_patterns:
    - pattern: '(?m)^###\s+Requirement:'
      type: regex
      level: error
      description: >-
        Brief must define at least one formal "### Requirement:" heading
        (see the Requirement/Scenario grammar in
        docs/agents/31_Owl_Requirement_Scenario_grammar.md).
  require_scenarios: true
  require_when_then: true
  # Enforced only at `owl step complete` (not by plain `owl artifact validate`):
  # a draft brief is well-formed, but the brief step cannot complete until the
  # user/agent records explicit approval in the front matter.
  completion_front_matter:
    status: approved
