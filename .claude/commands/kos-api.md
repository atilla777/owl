---
description: Use KOS API operations through the canonical KOS API skill
---
Load skill `kos-api`.

Use the command arguments as the requested KOS API operation or API workflow intent: $ARGUMENTS

Follow the skill instructions exactly. Resolve KOS bootstrap, project context, actor identity, lock versions, and API stop conditions from the skill before mutating application state.
