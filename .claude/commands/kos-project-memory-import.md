---
description: Migrate repository agent memory into KOS knowledge through the import skill
---
Load skill `kos-project-memory-import`.

Use the command arguments as project-memory import intent: $ARGUMENTS

Follow the skill instructions exactly. Import durable repository memory into KOS knowledge, verify retrieval through the API, create or update a Project Constitution article with `load_policy: required` and `required_project_bootstrap: true`, and only then perform any approved entrypoint switch-over.
