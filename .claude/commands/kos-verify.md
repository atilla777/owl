---
description: Run scoped verification for a KOS task
---
Load skill `kos-verify`.

Use the command arguments as verification intent for the active or specified KOS task: $ARGUMENTS

Follow the skill instructions exactly. Run the smallest relevant checks first, classify failures, and report whether the result can advance the KOS workflow.
