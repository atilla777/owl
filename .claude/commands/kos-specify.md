---
description: Produce or refine the KOS task spec (intent brief or full specification)
---
Load skill `kos-specify`.

Use the command arguments as specification intent for the active or specified KOS task: $ARGUMENTS

Follow the skill instructions exactly. Write the spec body that matches the task workflow type (intent_brief or specification) and persist it through the KOS API.
