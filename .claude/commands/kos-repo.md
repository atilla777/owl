---
description: Use KOS repository inspection, verification, and git safety operations
---
Load skill `kos-repo`.

Use the command arguments as repository operation intent for a KOS workflow: $ARGUMENTS

Follow the skill instructions exactly. Inspect status, preserve unrelated changes, run scoped verification, stage scoped files, and handle git safety rules.
