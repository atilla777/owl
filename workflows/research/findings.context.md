# Purpose

Investigate, gather data, write `findings.md` with Вопрос / Данные / Выводы.

## When to use

In `research` workflow after `question`.

## Inputs

- Research question.
- Source material (web, docs, code, conversations).

## Outputs

- `research_findings` artifact under `tasks/<TASK-ID>/findings.md`.
