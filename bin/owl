#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift(File.expand_path('../lib', __dir__))

require 'owl'

exit Owl::Cli::Api.run(
  argv: ARGV,
  stdout: $stdout,
  stderr: $stderr,
  env: ENV.to_h,
  cwd: Dir.pwd
)
